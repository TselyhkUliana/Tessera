<Window x:Class="Tessera.App.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tessera.App"
        xmlns:vm ="clr-namespace:Tessera.App.ViewModel"
        d:DataContext="{d:DesignData Type=vm:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="Harold" Height="450" Width="1000"
        WindowStartupLocation="CenterScreen" 
        TextOptions.TextFormattingMode="Display" FontFamily="Ascon Complex" SnapsToDevicePixels="True">

  <Window.Resources>
    <Style TargetType="DataGrid">
      <Setter Property="AutoGenerateColumns" Value="False"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="GridLinesVisibility" Value="None"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>
    <Style TargetType="DataGridRow">
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#dae6f2"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="DataGridCell">
      <Setter Property="BorderBrush" Value="#e0e0e0"/>
      <Setter Property="BorderThickness" Value="0 0 1 1"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="#ccdbeb"/>
          <Setter Property="Foreground" Value="#376cf0"/>
          <Setter Property="BorderBrush" Value="#ccdbeb"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style TargetType="ComboBox">
      <Setter Property="IsEditable" Value="True"/>
      <Setter Property="StaysOpenOnEdit" Value="True"/>
      <Setter Property="IsTextSearchEnabled" Value="False"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="TextBlockStyle">
      <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
      <Setter Property="Margin" Value="5 0 0 0"/>
    </Style>

    <DataTemplate x:Key="MaterialCell">
      <TextBlock Text="{Binding Material}" Style="{StaticResource TextBlockStyle}"/>
    </DataTemplate>
    <DataTemplate x:Key="MaterialCellEditing">
      <ComboBox ItemsSource="{Binding SuggestedMaterials}" Text="{Binding Material, UpdateSourceTrigger=PropertyChanged}" Loaded="ComboBoxMaterial_Loaded"/>
    </DataTemplate>
    <DataTemplate x:Key="SectionProfileCell">
      <TextBlock Text="{Binding SectionProfile}" Style="{StaticResource TextBlockStyle}"/>
    </DataTemplate>
    <DataTemplate x:Key="SectionProfileCellEditing">
      <ComboBox ItemsSource="{Binding SuggestedProfiles}" Text="{Binding SectionProfile, UpdateSourceTrigger=PropertyChanged}" Loaded="ComboBoxProfiles_Loaded"/>
    </DataTemplate>
  </Window.Resources>

  <DockPanel>
    <StackPanel DockPanel.Dock="Bottom">
      <Button Content="Создать" Command="{Binding Commands}" CommandParameter="{Binding DataContext.SectionDefinitions, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0 5 5 7" Padding="5 2"/>
    </StackPanel>
    <DataGrid x:Name="datagrid" ItemsSource="{Binding SectionDefinitions}" SelectedItem="{Binding CurrentSection}" Margin="5" BorderBrush="#e0e0e0">
      <DataGrid.Columns>
        <DataGridTemplateColumn Header="Материалы" CellTemplate="{StaticResource MaterialCell}" CellEditingTemplate="{StaticResource MaterialCellEditing}" Width="300"/>
        <DataGridTemplateColumn Header="Сортаменты" CellTemplate="{StaticResource SectionProfileCell}" CellEditingTemplate="{StaticResource SectionProfileCellEditing}" Width="350"/>
        <DataGridTextColumn Header="Типоразмеры" Binding="{Binding TypeSize, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</Window>