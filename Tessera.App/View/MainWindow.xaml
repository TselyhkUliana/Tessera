<Window x:Class="Tessera.App.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tessera.App"
        xmlns:vm ="clr-namespace:Tessera.App.ViewModel"
        xmlns:converters="clr-namespace:Tessera.App.Converters"
        d:DataContext="{d:DesignData Type=vm:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="Tessera" Height="550" Width="1200"
        WindowStartupLocation="CenterScreen" 
        TextOptions.TextFormattingMode="Display" FontFamily="Ascon Complex" SnapsToDevicePixels="True">

  <Window.Resources>
    <converters:MaterialHasStandardConverter x:Key="MaterialHasStandardConverter"/>
    
    <Style TargetType="DataGrid">
      <Setter Property="AutoGenerateColumns" Value="False"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="GridLinesVisibility" Value="None"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>
    <Style TargetType="DataGridRow">
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#dae6f2"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="DataGridCell" x:Key="DataGridCellStyle">
      <Setter Property="BorderBrush" Value="#e0e0e0"/>
      <Setter Property="BorderThickness" Value="0 0 1 1"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="#ccdbeb"/>
          <Setter Property="Foreground" Value="#376cf0"/>
          <Setter Property="BorderBrush" Value="#ccdbeb"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="DataGridCell" x:Key="MaterialCellStyle" BasedOn="{StaticResource DataGridCellStyle}">
      <Style.Triggers>
        <DataTrigger Binding="{Binding Material, Converter={StaticResource MaterialHasStandardConverter}}" Value="False">
          <Setter Property="Background" Value="#faf8ca"/>
          <Setter Property="ToolTip" Value="Материал должен содержать стандарт (например, ГОСТ, ISO и т.д.)"/>
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style TargetType="ComboBox">
      <Setter Property="IsEditable" Value="True"/>
      <Setter Property="StaysOpenOnEdit" Value="True"/>
      <Setter Property="IsTextSearchEnabled" Value="False"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="TextBlockStyle">
      <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
      <Setter Property="Margin" Value="5 0 0 0"/>
    </Style>

    <DataTemplate x:Key="MaterialCell">
      <TextBlock Text="{Binding Material}" Style="{StaticResource TextBlockStyle}"/>
    </DataTemplate>
    <DataTemplate x:Key="MaterialCellEditing">
      <ComboBox ItemsSource="{Binding SuggestedMaterials}" Text="{Binding Material, UpdateSourceTrigger=PropertyChanged}" Loaded="ComboBoxMaterial_Loaded"/>
    </DataTemplate>
    <DataTemplate x:Key="SortamentCell">
      <TextBlock Text="{Binding Sortament}" Style="{StaticResource TextBlockStyle}"/>
    </DataTemplate>
    <DataTemplate x:Key="SortamentCellEditing">
      <ComboBox ItemsSource="{Binding SuggestedSortament}" Text="{Binding Sortament, UpdateSourceTrigger=PropertyChanged}" Loaded="ComboBoxProfiles_Loaded"/>
    </DataTemplate>
  </Window.Resources>

  <DockPanel>
    <UniformGrid DockPanel.Dock="Top" Margin="5"  HorizontalAlignment="Left" Rows="1">
      <UniformGrid.Resources>
        <Style TargetType="Button">
          <Setter Property="Margin" Value="0 0 5 0"/>
          <Setter Property="Padding" Value="5 2"/>
        </Style>
      </UniformGrid.Resources>
      <Button Command="{Binding AddFileForMaterialCommand}" CommandParameter="{Binding CurrentSection}" ToolTip="{Binding AddFileForMaterialCommand.Hint}" Background="Transparent">
        <StackPanel Orientation="Horizontal">
          <Image Source="{Binding AddFileForMaterialCommand.Icon}" Width="16" Height="16" />
          <TextBlock Text="{Binding AddFileForMaterialCommand.Caption}" />
        </StackPanel>
      </Button>
      <Button Command="{Binding AddFileForSortamentCommand}" CommandParameter="{Binding CurrentSection}" ToolTip="{Binding AddFileForSortamentCommand.Hint}" Background="Transparent">
        <StackPanel Orientation="Horizontal">
          <Image Source="{Binding AddFileForSortamentCommand.Icon}" Width="16" Height="16"/>
          <TextBlock Text="{Binding AddFileForSortamentCommand.Caption}"/>
        </StackPanel>
      </Button>
    </UniformGrid>
    <StackPanel DockPanel.Dock="Bottom">
      <Button Content="Создать" Command="{Binding CreateCommand}" CommandParameter="{Binding SectionDefinitions}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0 5 5 7" Padding="5 2"/>
    </StackPanel>
    <DataGrid x:Name="datagrid" ItemsSource="{Binding SectionDefinitions}" SelectedItem="{Binding CurrentSection}" Margin="5" BorderBrush="#e0e0e0">
      <DataGrid.Columns>
        <DataGridTemplateColumn Header="Материалы" CellTemplate="{StaticResource MaterialCell}" CellStyle="{StaticResource MaterialCellStyle}" CellEditingTemplate="{StaticResource MaterialCellEditing}" Width="300"/>
        <DataGridTemplateColumn Header="Сортаменты" CellTemplate="{StaticResource SortamentCell}" CellStyle="{StaticResource DataGridCellStyle}" CellEditingTemplate="{StaticResource SortamentCellEditing}" Width="350"/>
        <DataGridTextColumn Header="Типоразмеры" Binding="{Binding TypeSize, UpdateSourceTrigger=PropertyChanged}" CellStyle="{StaticResource DataGridCellStyle}" Width="150"/>
        <DataGridTextColumn Header="Экземпляры сортаментов" Binding="{Binding SectionInstance}" CellStyle="{StaticResource DataGridCellStyle}" Width="*"/>
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</Window>